<div class="loop-visual" role="img" aria-label="Agent 工作循环：计划、执行、验证、改进，并记录可追踪的运行状态">
  <div class="loop-header">
    <span>agent.session</span>
    <span class="run-state"><i></i> walkthrough</span>
  </div>
  <div class="loop-canvas" aria-hidden="true">
    <div class="goal-card">
      <span>GOAL</span>
      <strong>reliable completion</strong>
      <p>bounded · observable · evidence-first</p>
    </div>
    <div class="loop-grid">
      <div class="loop-node" style="--order: 0"><i>01</i><span>Plan</span><em>拆解目标</em></div>
      <div class="loop-node" style="--order: 1"><i>02</i><span>Act</span><em>调用工具</em></div>
      <div class="loop-node" style="--order: 3"><i>04</i><span>Improve</span><em>有界修订</em></div>
      <div class="loop-node" style="--order: 2"><i>03</i><span>Verify</span><em>证据校验</em></div>
    </div>
    <div class="trace-row"><span>trace</span><i></i><b>checkpoint visible</b><em>bounded</em></div>
  </div>
</div>

<style>
  .loop-visual { position: relative; overflow: hidden; border: 1px solid rgb(100 116 139 / .18); border-radius: 1.75rem; background: linear-gradient(145deg, rgb(255 255 255 / .94), rgb(245 247 251 / .92)); box-shadow: 0 30px 80px rgb(31 41 55 / .12); }
  .loop-visual::before { position: absolute; width: 18rem; height: 18rem; border-radius: 50%; background: #c9c3ff; content: ''; filter: blur(80px); opacity: .25; right: -8rem; top: -8rem; }
  .loop-header { position: relative; display: flex; min-height: 2.9rem; align-items: center; justify-content: space-between; border-bottom: 1px solid rgb(100 116 139 / .14); padding: 0 1.1rem; color: #64748b; font: 600 .66rem/1 var(--font-mono); }
  .run-state { display: inline-flex; align-items: center; gap: .38rem; }
  .run-state i { width: .4rem; height: .4rem; border-radius: 50%; background: #19a37d; box-shadow: 0 0 0 0 rgb(25 163 125 / .28); animation: running 2s ease-out infinite; }
  .loop-canvas { position: relative; padding: 1rem; }
  .goal-card { position: relative; z-index: 2; border: 1px solid #dedcfb; border-radius: 1rem; background: rgb(255 255 255 / .88); padding: 1rem; box-shadow: 0 12px 28px rgb(88 72 218 / .08); }
  .goal-card span { color: #6558f5; font: 650 .58rem/1 var(--font-mono); letter-spacing: .12em; }
  .goal-card strong { display: block; margin-top: .45rem; color: #111827; font-size: 1rem; letter-spacing: -.02em; }
  .goal-card p { margin-top: .35rem; color: #64748b; font: 500 .62rem/1.4 var(--font-mono); }
  .loop-grid { position: relative; display: grid; grid-template-columns: repeat(2, 1fr); gap: .65rem; margin-top: .7rem; }
  .loop-grid::before, .loop-grid::after { position: absolute; z-index: 0; background: linear-gradient(90deg, transparent, #a99fff, transparent); content: ''; opacity: .42; }
  .loop-grid::before { width: 70%; height: 1px; left: 15%; top: 50%; }
  .loop-grid::after { width: 1px; height: 70%; left: 50%; top: 15%; background: linear-gradient(transparent, #a99fff, transparent); }
  .loop-node { position: relative; z-index: 1; display: grid; grid-template-columns: 1.65rem 1fr; gap: .1rem .6rem; border: 1px solid #e5e7eb; border-radius: .9rem; background: rgb(255 255 255 / .94); padding: .75rem; animation: node-cycle 6s ease-in-out infinite; animation-delay: calc(var(--order) * 1.35s); }
  .loop-node i { grid-row: span 2; display: grid; width: 1.65rem; height: 1.65rem; place-items: center; border-radius: .55rem; color: #6558f5; background: #eeecff; font: 650 .55rem/1 var(--font-mono); font-style: normal; }
  .loop-node span { color: #273244; font-size: .73rem; font-weight: 700; }
  .loop-node em { color: #7b8796; font-size: .58rem; font-style: normal; }
  .trace-row { display: grid; grid-template-columns: auto .42rem 1fr auto; align-items: center; gap: .5rem; margin-top: .7rem; border-radius: .75rem; color: #64748b; background: #111827; padding: .72rem .8rem; font: 500 .56rem/1 var(--font-mono); }
  .trace-row span { color: #a99fff; }
  .trace-row i { width: .36rem; height: .36rem; border-radius: 50%; background: #34d399; }
  .trace-row b { overflow: hidden; color: #d5dbea; font-weight: 500; text-overflow: ellipsis; white-space: nowrap; }
  .trace-row em { color: #94a3b8; font-style: normal; }

  :global(.dark) .loop-visual { border-color: rgb(148 163 184 / .18); background: linear-gradient(145deg, rgb(15 23 37 / .96), rgb(8 13 23 / .92)); box-shadow: 0 30px 80px rgb(0 0 0 / .28); }
  :global(.dark) .loop-header { border-color: #263244; }
  :global(.dark) .goal-card, :global(.dark) .loop-node { border-color: #2b3850; background: rgb(17 27 43 / .92); }
  :global(.dark) .goal-card strong, :global(.dark) .loop-node span { color: #edf2fa; }
  :global(.dark) .goal-card p, :global(.dark) .loop-node em { color: #9aa8ba; }
  :global(.dark) .loop-node i { color: #c8c2ff; background: rgb(101 88 245 / .18); }

  @keyframes running { 70%, 100% { box-shadow: 0 0 0 .45rem transparent; } }
  @keyframes node-cycle { 0%, 18%, 100% { border-color: #e5e7eb; box-shadow: none; transform: translateY(0); } 28%, 48% { border-color: #a99fff; box-shadow: 0 10px 26px rgb(101 88 245 / .13); transform: translateY(-.12rem); } }

  @media (max-width: 460px) {
    .loop-canvas { padding: .75rem; }
    .loop-grid { gap: .45rem; }
    .loop-node { grid-template-columns: 1.4rem 1fr; gap: .1rem .45rem; padding: .6rem; }
    .loop-node i { width: 1.4rem; height: 1.4rem; }
  }

  @media (prefers-reduced-motion: reduce) {
    .loop-visual * { animation: none !important; }
  }
</style>
